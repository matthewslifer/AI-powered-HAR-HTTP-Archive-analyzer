[project]
name = "har-mcp"
version = "0.1.0"
description = "MCP server for analyzing HAR files and generating HTTP failure traces"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "requests",
    "mcp-server"
]
